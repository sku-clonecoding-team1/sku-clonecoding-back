spring:

  datasource:
    url: jdbc:mysql://localhost:3306/moviecgv?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: tiger
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

    properties:
      hibernate:
        show_sql: true
        format_sql: true
#    database-platform:

logging:
  level:
    org.springframework.security: DEBUG

